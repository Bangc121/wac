# Features

This directory contains the feature-based modules of the application.

Each feature should ideally contain its own:
- `data/`: Repositories, Data Sources, Models.
- `presentation/`: Screens (Pages), Widgets, State Management (Providers/Blocs).
- `domain/`: Entities, Use Cases (if using Clean Architecture strictly).
